# lab_5

**Погодный сервис** — это современный метео‑сервис, в котором бэкенд на Go и PostgreSQL автоматически собирает и обновляет погодные данные для городов всего мира по расписанию через cron. Сервис интегрируется с Open-Meteo Weather API и Geocoding API, чтобы получать актуальную температуру, условия и выполнять поиск городов с автодополнением по названию. Бэкенд использует gocron для фоновых задач, pgx и sql-migrate для работы с базой и миграций, а также in-memory кэш для ускорения ответов. Фронтенд на TypeScript и React с Material UI и React-Leaflet отображает погоду в виде адаптивных карточек, карусели популярных городов и интерактивной карты мира. Пользователь может найти город через строку поиска с подсказками, выбрать его щелчком на карте и сразу увидеть текущий прогноз. API предоставляет удобные эндпоинты для поиска города по текстовому запросу и получения погоды по названию города. Проект ориентирован на наглядный и удобный интерфейс, сочетающий красивый дизайн, карту и мощный Go-бэкенд для стабильной работы в продакшне.



**Sprint Review**
В первом спринте команда успешно завершила две ключевые задачи из пяти запланированных: Issue #1 (7-дневный прогноз погоды) и Issue #4 (оптимизация с Redis). По Issue #1 интегрировали Open-Meteo Forecast API, добавили миграцию таблицы forecasts, обновили cron для ежедневной загрузки, написали unit-тесты (покрытие 85%) и обновили фронт с графиком на React-Leaflet — теперь пользователи видят детальный прогноз на неделю с почасовыми данными. Issue #4 ввел Redis в docker-compose, реализовал TTL-кэширование (hit-rate 92%), провел нагрузочные тесты (ab -n 1000), что ускорило API в 2.3 раза под 100 RPS. Итог спринта: доставлена стабильная основа для масштабирования — прогноз работает в проде, кэш снижает нагрузку на внешние API. Не успели завершить Issue #3 (алерты: задержка из-за undocumented rate-limits Open-Meteo, частично готов эндпоинт) и Issue #5 (история: Chart.js интегрирован, но график не responsive на мобайл — ожидание фикса MUI темы). Общий velocity: 7 story points из 15 (47%), демо показано стейкхолдерам с живой картой и прогнозом.

**Sprint Retrospective**
Что прошло хорошо: Docker Compose радикально упростил локальную разработку — docker compose up запускает стек за 30 сек, тесты в CI (GitHub Actions) проходят автоматически на каждом PR (4 пуша/день), фронт-бэк интеграция seamless благодаря Swagger-докам. Команда синхронизирована через daily standups в Issues комментах.
​

Что пошло не так: Rate-limits Open-Meteo (60 req/min) заблокировали cron-тесты на 2 дня, вынудив ручной mock; Issue #3 зависло из-за отсутствия fallback API. Фронт-разработчик потратил лишние часы на MUI-стили (тема конфликтовала с Leaflet). Нет четкого DoR (Definition of Ready) — задачи в спринт брали сырыми.

Что улучшить в следующем спринте: Внедрить DoR checklist в issue-шаблоны (моки/API keys готовы?); добавить второе API (WeatherAPI.com) как fallback; выделить 20% времени на спайк (исследование Chart.js); сократить спринт до 7 дней; мониторить velocity в Projects board; больше pair-programming для фронта. План: фокус на Issue #3/#5 + новые (CI/CD), цель 12 SP.
​

